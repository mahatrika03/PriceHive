// Get the button element
const button = document.querySelector('.button');

// Add a click event listener to the button
button.addEventListener('click', () => {
  // Redirect to pricecom.html
  window.location.href = 'pricecom.html';
});